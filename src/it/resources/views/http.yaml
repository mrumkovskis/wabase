name: http_test
api: post
decoder: none
post:
- body = extract entity
- http post {'data', 'http_echo'} :body


# Same swagger shape as http_test, no overrides — keep only the first in swagger.
name: http_echo
api: post
decoder: none
post:
- extract entity
swagger:
  paths = : {}

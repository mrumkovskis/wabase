name:   person_with_access_control
table:  person
api:    manager get
paths:  /data/person_with_access_control
key:    code
fields:
- code
- name
- surname
order:
- code

name: has_role
get: exists(user_role[user_id = :current_user.id & role in :roles] {1})

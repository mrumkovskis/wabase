on: /data/((?:create:|count:)?\w+)(/.+)?
do: authenticateOpt doAction $1
swagger:
  paths = : {}    # skip here - derived from views

on: GET /qe_call/guideline_calculation_helper
do: authenticateOpt wabase.app.Guidelines.qeCall
swagger:
  '200':
    content:
      application/json:
        type: object

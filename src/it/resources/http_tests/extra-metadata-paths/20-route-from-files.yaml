# route def loaded from a subdirectory of a configured path is served - path scan is recursive
path:     /extra-metadata-file-hello
response_headers:
  Content-Type: text/plain; charset=UTF-8
response: "Hello from extra metadata files!"

path:       /data/validation/100
method:     PUT
request:
  context:     save_person_email
  expression:  email && email.length <= 75
  message:     "'Email value exceeds 75 symbols - ' + email"
response:
